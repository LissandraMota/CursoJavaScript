const exportar = require("./exportar")
console.log(exportar)

// o que foi exportado estava dentro de module.exports
// portanto para atribuir um novo objeto para exportação não pode usar só o exports

const exportar2 = require("./exportar")